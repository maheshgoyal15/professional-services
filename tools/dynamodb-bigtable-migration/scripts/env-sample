# =========================================
# GCP Project & Infrastructure Settings
# =========================================
PROJECT_ID=<your-gcp-project-id>
REGION=<your-gcp-region>

# Artifact Registry settings
REPOSITORY=<your-artifact-repo-name>
IMAGE_NAME=dynamodb-to-bigtable:latest

# =========================================
# GCS Bucket Locations
# =========================================
# It is common to use the same bucket for all Dataflow artifacts,
# just separated by prefixes (folders).
BUCKET_NAME=<your-gcs-bucket-name>

FLEX_TEMPLATE_SPEC_FILE_PATH="gs://${BUCKET_NAME}/templates/dynamodb-to-bigtable.json"
STAGING_LOCATION="gs://${BUCKET_NAME}/staging"
TEMP_LOCATION="gs://${BUCKET_NAME}/temp"

# =========================================
# Input & Control Configuration
# =========================================
CONTROL_FILE_PATH="gs://${BUCKET_NAME}/config/control.json"

# Optional override if you want to test specific files instead of
# what is defined in the control.json 'sourceFileLocation'
INPUT_FILEPATH="gs://${BUCKET_NAME}/dynamodb-export/AWSDynamoDB/*.json.gz"

# =========================================
# Bigtable Target Configuration
# =========================================
BIGTABLE_INSTANCE_ID=<your-bigtable-instance-id>
BIGTABLE_TABLE_ID=<your-bigtable-table-id>